<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar_progress_load"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        style="@style/ToolbarTheme"
        app:titleTextColor="@color/white"
        android:background="@color/colorPrimary" />

    <TextView
        android:id="@+id/tv_progress_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Loading your progress"
        android:textSize="20sp"
        android:layout_centerInParent="true"/>
    
    <ProgressBar
        android:layout_width="match_parent"
        android:layout_height="17dp"
        android:layout_below="@id/tv_progress_title"
        android:layout_alignStart="@id/tv_progress_title"
        android:layout_alignLeft="@id/tv_progress_title"
        android:layout_alignEnd="@id/tv_progress_title"
        android:layout_alignRight="@id/tv_progress_title"
        android:layout_marginTop="10dp"
        android:indeterminate="true"
        style="?android:attr/progressBarStyleHorizontal"/>
</RelativeLayout>